# ============================================
# AI Agent — Dependencies
# ============================================

# Frontend
streamlit>=1.30.0

# Optional REST API backend (server.py)
fastapi>=0.110.0
uvicorn>=0.27.0

# HTTP client for OpenRouter API
httpx>=0.25.0

# Environment variables
python-dotenv>=1.0.0

# Web search (free, no API key needed)
# NOTE: `duckduckgo-search` was renamed to `ddgs`; the old package still
# imports but returns zero results, so `ddgs` is required.
ddgs>=9.0.0

# Safe math evaluation
sympy>=1.12

# Document reading (PDF / DOCX / XLSX)
PyPDF2>=3.0.0
openpyxl>=3.1.0

# HTML parsing & extraction for the URL reader
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdownify>=0.13.0

# Data analysis inside the Python sandbox and CSV profiling
numpy>=1.26.0
pandas>=2.0.0

# Data models
pydantic>=2.0.0

# === Database Stack ===

# Supabase PostgreSQL (cloud-persistent memory)
supabase>=2.0.0

# Redis (response & tool caching)
redis>=5.0.0

# === Vector Databases (RAG — choose one or more) ===

# Pinecone (cloud vector search with integrated embeddings)
pinecone>=5.0.0

# Weaviate (cloud vector search with built-in vectorizer)
weaviate-client>=4.0.0

# Qdrant (cloud vector search with FastEmbed)
qdrant-client[fastembed]>=1.9.0

# === MCP (Model Context Protocol) ===

# MCP SDK for connecting to external tool servers
mcp>=1.0.0
